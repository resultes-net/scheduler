apiVersion: apps/v1
kind: Deployment
metadata:
  name: scheduler

  annotations:
    keel.sh/policy: force
    keel.sh/trigger: poll
    keel.sh/match-tag: true

spec:
  replicas: 1
  selector:
    matchLabels:
      resultes.net/component: scheduler
  template:
    metadata:
      labels:
        resultes.net/component: scheduler
    spec:
      containers:
        - image: ghcr.io/resultes-net/scheduler:latest
          name: scheduler
          env:
            - name: SERVER_HOST
              value: internal-server.server.svc.cluster.local
            - name: SERVER_PORT
              value: "8000"
            - name: RUNNER_HOST
              value: 172.21.0.115
            - name: RUNNER_PORT
              value: "3000"
            - name: USE_OPENSTACK
              value: "1"
            - name: OS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: os-password-secret
                  key: os-password

                            
      imagePullSecrets:
        - name: read-ghcr-io-auth-secret
      restartPolicy: Always
